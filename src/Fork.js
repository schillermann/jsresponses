/**
 * Fork for ResponseForked.
 * @typedef {Object} Fork
 * @property {function(import('./Request.js').Request): boolean} matches Does it match the request?
 * @property {function(import('./Request.js').Request): import('./Response.js').Response} response The response
 */
