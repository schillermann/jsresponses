/**
 * HTTP request line.
 * @typedef {Object} RequestLine
 * @property {function(): string} method Method (e.g., GET)
 * @property {function(): string} path Path (e.g., /index.html)
 * @property {function(): string} query Query string (e.g., name=mario)
 * @property {function(): string} protocol Protocol (e.g., HTTP/1.1)
 */
